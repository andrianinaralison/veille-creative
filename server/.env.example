ANTHROPIC_API_KEY=sk-ant-...
DATABASE_URL=postgresql://user:pass@localhost:5432/veillecreative
DIRECT_URL=postgresql://user:pass@localhost:5432/veillecreative

# ── Auth ──────────────────────────────────────────────────────────────────────
JWT_SECRET=changeme_au_moins_32_caracteres_aleatoires
JWT_EXPIRES_IN=7d
# Générer avec : node -e "import('bcrypt').then(b => b.default.hash('TON_MOT_DE_PASSE', 12).then(console.log))"
ADMIN_PASSWORD_HASH=

FRONTEND_URL=http://localhost:5173
NODE_ENV=development
PORT=3001

# ── YouTube Data API v3 (Google Cloud Console) ────────────────────────────────
YOUTUBE_API_KEY=AIza...

# ── Supabase Storage (Settings → API dans le dashboard Supabase) ─────────────
SUPABASE_URL=https://xxxx.supabase.co
# Service role key (PAS l'anon key) — ne jamais exposer côté frontend
SUPABASE_SERVICE_KEY=eyJ...
SUPABASE_STORAGE_BUCKET=thumbnails
